openapi: 3.0.0
info:
  version: 1.0.0
  title: REST Shipping
  license:
    name: GNU Affero General Public License v3.0
paths:
  /info:
    get:
      summary: /info
      description: Optional extended description in CommonMark or HTML.
      responses:
        "200": # status code
          description: A JSON array of user names
          content:
            application/json:
              schema:
                type: array
                items:
                  type: string
  /provider/dpd/getLabel:
    post:
      summary: /provider/dpd/getLabel
      description: Optional extended description in CommonMark or HTML.
      responses:
        "200": # status code
          description: A JSON array of user names
          content:
            application/json:
              schema:
                type: array
                items:
                  type: string
